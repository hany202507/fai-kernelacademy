# 기여 가이드

FAI 인증과정 저장소에 기여하기 전에 이 문서를 읽어주세요. 특히 **1절은 예외가 없습니다.**

---

## 1. 실데이터는 절대 반입하지 않습니다

이 저장소는 **공개 예정**입니다. 그리고 git 은 한 번 커밋된 내용을 지워도 히스토리에 남깁니다. 파일을 삭제하는 커밋을 올려도, 이전 커밋을 되짚으면 그대로 보입니다. 히스토리에서 완전히 지우려면 저장소를 다시 쓰고 모든 클론을 폐기해야 합니다.

### 반입 금지 대상

- 실제 고객사·거래처의 **상호, 사업자등록번호, 대표자명**
- **주민등록번호, 여권번호, 운전면허번호**
- **계좌번호, 카드번호**
- 실제 회사의 **원장·전표·급여대장·세금계산서** 원본 또는 그 일부
- 실제 금액이 담긴 재무제표·시산표
- 고객과의 메일·메신저 내용

교육 자료를 만들다 보면 "실제 사례가 있어야 설득력이 있다"고 느끼게 됩니다. 그때가 위험한 순간입니다. **사례는 구조만 가져오고 숫자와 이름은 전부 바꿉니다.**

### 실습 데이터는 가상 회사로

`canon/10_예시실습/회사프로필.md` 의 **메종블룸**이 정본입니다. 1년치 원장·카드승인·통장·세금계산서·급여대장을 한 세트로 갖추는 것을 목표로 확장하고 있습니다.

새 실습이 필요하면 메종블룸을 확장하세요. 다른 가상 회사를 새로 만들면 데이터셋이 갈라지고, 갈라진 데이터셋은 아무도 안 씁니다.

### 자동 검사 — 켜야 작동합니다

커밋 직전에 스테이징된 내용을 스캔하는 훅이 있습니다. **클론마다 한 번 활성화해야 합니다.**

```bash
git config core.hooksPath .githooks
```

무엇을 잡는지:

| 검사 | 형태 (`#` = 숫자) |
|---|---|
| 주민등록번호 | `YYMMDD-N######` (N은 1~4) |
| 사업자등록번호 | `###-##-#####` |
| 카드번호 | `####-####-####-####` (하이픈 또는 공백) |
| 파일명 | `실데이터`, `real_data`, `고객사`, `클라이언트` 포함 |

> 이 표에 실제 자리수의 예시 숫자를 적으면 훅이 이 문서 자체를 차단합니다. 실제로 처음 작성할 때 그렇게 걸렸습니다. 형태만 적으세요.

오탐이면 `git commit --no-verify` 로 넘길 수 있습니다. 다만 **넘기기 전에 왜 걸렸는지 한 번은 보세요.**

**훅이 못 잡는 것:**

- **docx·xlsx·pdf 안의 내용.** 텍스트만 스캔하므로 첨부 문서 속 실데이터는 통과합니다. 바이너리는 사람이 확인해야 합니다.
- 이름·상호 같은 **패턴 없는 개인정보**. `주식회사 실제상호` 는 아무 규칙에도 안 걸립니다.

즉 훅은 **부주의를 잡는 장치**이고, 판단을 대신하지 않습니다.

### 이미 올렸다면

되돌리려 하지 말고 **즉시 박상정에게 알리세요.** `git rm` 이나 새 커밋으로는 해결되지 않고, 늦어질수록 다른 사람이 클론해 갈 가능성이 커집니다.

---

## 2. 실습 산출물 규격

실습 결과는 **"AI 와 대화한 기록"이 아닙니다.** 아래 3종을 함께 냅니다.

| 산출물 | 내용 |
|---|---|
| **재실행 가능한 스크립트** | 같은 입력을 넣으면 같은 결과가 나오는 코드 |
| **산출 파일** | 그 스크립트가 만든 결과물 |
| **검증 결과** | 결과가 맞다는 근거 (테스트 출력, tie-out, 대조표) |

이 규격은 교육 방법론 자체입니다. **회사 양식·언어는 코드로 고정하고 AI 는 판단·추론에만 쓴다** — 이 결정론 프레임을 손으로 익히게 하는 것이 목적입니다. 대화록만 내면 재현되지 않고, 재현되지 않으면 실무에 못 씁니다.

---

## 3. PR 로만 바꿉니다

커리큘럼·평가기준·표준 문서는 직접 push 하지 않고 PR 로 올립니다. 운영진이 서로의 변경을 쉽게 확인할 수 있어야 한다는 요구에서 나온 규칙입니다.

- 무엇을 왜 바꾸는지 PR 본문에 씁니다
- 기존 문서와 어긋나는 내용이면 **덮어쓰지 말고 어긋난다는 사실을 먼저 적습니다.** 어느 쪽이 정본인지는 논의해서 정합니다
- 리서치 수치를 넣을 때는 **출처와 확인일**을 함께 씁니다 (`03_리서치_국내시장_민간자격.md` 형식 참조)

---

## 4. 문서를 쓸 때

- 근거 없는 수치를 쓰지 않습니다. 교차검증이 안 되면 **"미검증"으로 표시**하고 대외 자료에는 쓰지 않습니다
- 회의록·전사(ASR)의 자동요약을 그대로 믿지 않습니다. 요약이 결론을 바꿔놓은 사례가 이미 있습니다 (`_handover-2026-07-29.md` §2 참조)
- 성함이 확실하지 않으면 **미확정으로 남깁니다.** 추측해서 적으면 그게 굳습니다

---

## 5. 공개 범위

파일을 추가할 때 어느 등급인지 확인하세요.

| 등급 | 내용 |
|---|---|
| Public | 개념 문서, 기능 카탈로그, 실습 데이터셋, 에이전트 설계서 스켈레톤 |
| 수강생 전용 | 완성 설계서, 정답 스크립트, 검증 하네스 |
| **Private** | 강의 대본, 평가·인증 기준, 가격·운영 매뉴얼, **제안·협상 문서** |

제안·협상 문서는 Private 입니다. 외부 협업자를 초대하기 전에 이 분리가 확정돼 있어야 합니다.

---

## 문의

박상정 (ptsjms85@gmail.com)
